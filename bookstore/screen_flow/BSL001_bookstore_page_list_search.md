```mermaid
graph TD
    A[書店ページ一覧ページを開く] --> B[初期データ読み込み]
    B --> C{データ取得成功?}
    C -- 成功 --> D[書店ページ一覧表示]
    C -- 失敗 --> E[エラーメッセージ表示]
    E --> F[再読み込みボタン]
    F --> B
    
    D --> G[ユーザーアクション]
    
    G --> H[検索条件入力]
    H --> I[検索実行]
    I --> J{検索成功?}
    J -- 成功 --> K{結果あり?}
    K -- あり --> D
    K -- なし --> L[該当データなしメッセージ]
    J -- 失敗 --> M[検索エラーメッセージ]
    
    G --> N[ページネーション]
    N --> O[次/前/指定ページデータ取得]
    O --> P{取得成功?}
    P -- 成功 --> D
    P -- 失敗 --> Q[ページ切替エラーメッセージ]
    
    G --> R[表示形式切替]
    R --> S[テーブル/カード表示切替]
    S --> D
    
    G --> T[ソート条件変更]
    T --> U[データ再ソート]
    U --> D
    
    G --> V[一括操作]
    V --> W[項目選択]
    W --> X[操作選択と実行]
    X --> Y{操作成功?}
    Y -- 成功 --> Z[操作成功メッセージ]
    Z --> B
    Y -- 失敗 --> AA[操作エラーメッセージ]
    
    G --> AB[個別操作]
    AB --> AC[詳細表示/編集/削除]
    AC --> AD{操作種別?}
    AD -- 詳細表示 --> AE[詳細画面へ遷移]
    AD -- 編集 --> AF[編集画面へ遷移]
    AD -- 削除 --> AG[削除確認ダイアログ]
    AG --> AH{確認結果}
    AH -- キャンセル --> D
    AH -- 削除実行 --> AI{削除成功?}
    AI -- 成功 --> AJ[削除成功メッセージ]
    AJ --> B
    AI -- 失敗 --> AK[削除エラーメッセージ]
    
    G --> AL[新規書店ページ作成]
    AL --> AM[新規登録画面へ遷移]